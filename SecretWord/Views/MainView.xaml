<Window x:Class="SecretWord.Views.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SecretWord"
        mc:Ignorable="d"
        Title="MainWindow" Height="350" Width="525">
    <Border>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition/>
            </Grid.RowDefinitions>
            <TextBox Grid.Row="1" Text="{Binding Doc.Text}" AcceptsReturn="True" TextWrapping="WrapWithOverflow"/>
            <Button Content="Test" Command="{Binding TestClick}"/>
        </Grid>
    </Border>
</Window>
